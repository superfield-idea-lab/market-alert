name: Release — cli
on:
  push:
    tags:
      - 'v*'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "scaffold placeholder — replace with real build implementation"
  publish:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - run: echo "scaffold placeholder — replace with real publish implementation"
