# calypso-starter

Core project documentation lives in [docs/README.md](docs/README.md).

Canonical blueprint documentation is sourced from the `./calypso-blueprint` git submodule while the repo keeps its workflow YAMLs under `agent-context/workflows/`. Run `git submodule update --init --remote calypso-blueprint` to refresh everything in a fresh clone.
