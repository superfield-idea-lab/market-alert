# release.yml — Build and push the Calypso container image on a version tag.
#
# Release and deploy are intentionally separate concerns:
#   - release.yml produces the container image artifact (tags: v*)
#   - deploy.yml applies the image to an environment (workflow_dispatch)
#
# This workflow does NOT automatically trigger a deployment. After a successful
# release, the operator manually triggers .github/workflows/deploy.yml with the
# desired image tag and target environment. This allows:
#   - Production deploys to require reviewer approval (GitHub Environment rules)
#   - Rollback to an earlier image tag without creating a new release
#   - Re-deployment of the same tag for config-only changes
#
# See docs/host-init-script.md for the full deployment runbook.

name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GH_SUBMODULE_PAT }}
      - uses: oven-sh/setup-bun@v2
      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts
      - name: Lint and format check
        run: bun run lint
      - name: Run unit tests
        run: bun --bun vitest run tests/unit apps/*/tests/unit

  api-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GH_SUBMODULE_PAT }}
      - uses: oven-sh/setup-bun@v2
      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts
      - name: Run API integration tests
        run: bun --bun vitest run --config apps/server/vitest.integration.config.ts

  component-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GH_SUBMODULE_PAT }}
      - uses: oven-sh/setup-bun@v2
      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts
      - name: Install Playwright Chromium
        run: bunx playwright install --with-deps chromium
      - name: Run component tests
        run: bun --bun vitest run --config apps/web/vitest.browser.config.ts

  e2e-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GH_SUBMODULE_PAT }}
      - uses: oven-sh/setup-bun@v2
      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts
      - name: Install Playwright Chromium
        run: bunx playwright install --with-deps chromium
      - name: Run E2E tests
        run: bun --bun vitest run --config tests/e2e/vitest.config.ts

  migration-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive
          token: ${{ secrets.GH_SUBMODULE_PAT }}
      - uses: oven-sh/setup-bun@v2
      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts
      - name: Run migration smoke tests
        run: bun run test:migration

  build-frontend:
    needs: [unit-tests, api-tests, component-tests, e2e-tests, migration-tests]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GH_SUBMODULE_PAT }}

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts

      - name: Build frontend bundle
        run: cd apps/web && bun --bun vite build

      - name: Create release tarball
        run: tar -czf web-dist.tar.gz -C apps/web/dist .

      - name: Upload web-dist artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-dist
          path: web-dist.tar.gz

      - name: Create GitHub Release
        run: |
          gh release delete "${{ github.ref_name }}" --yes 2>/dev/null || true
          gh release create "${{ github.ref_name }}" web-dist.tar.gz --title "${{ github.ref_name }}" --generate-notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-container:
    needs: [build-frontend]
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: app_rw
          POSTGRES_PASSWORD: app_rw_password
          POSTGRES_DB: calypso_app
        options: >-
          --health-cmd "pg_isready -U app_rw -d calypso_app"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GH_SUBMODULE_PAT }}

      - name: Download web-dist artifact
        uses: actions/download-artifact@v4
        with:
          name: web-dist

      - name: Extract web-dist
        run: |
          mkdir -p apps/web/dist
          tar -xzf web-dist.tar.gz -C apps/web/dist

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build container image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.release
          load: true
          push: false
          tags: |
            ghcr.io/${{ github.repository }}:${{ github.ref_name }}
            ghcr.io/${{ github.repository }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          no-cache: true

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts

      - name: Initialize databases
        run: bun run packages/db/init-remote.ts
        env:
          ADMIN_DATABASE_URL: postgres://app_rw:app_rw_password@localhost:5432/calypso_app
          APP_RW_PASSWORD: app_rw_password
          AUDIT_W_PASSWORD: audit_w_password
          ANALYTICS_W_PASSWORD: analytics_w_password
          AGENT_CODING_PASSWORD: agent_coding_password
          AGENT_ANALYSIS_PASSWORD: agent_analysis_password

      - name: Start app container
        run: |
          docker run -d \
            --name calypso \
            --network host \
            -v "$PWD/packages/db/schema.sql:/app/dist/schema.sql:ro" \
            -e DATABASE_URL=postgres://app_rw:app_rw_password@localhost:5432/calypso_app \
            -e AUDIT_DATABASE_URL=postgres://audit_w:audit_w_password@localhost:5432/calypso_audit \
            -e ANALYTICS_DATABASE_URL=postgres://analytics_w:analytics_w_password@localhost:5432/calypso_analytics \
            -e JWT_SECRET=ci-smoke-test-secret \
            -e PORT=31415 \
            -e RELEASE_TAG=${{ github.ref_name }} \
            ghcr.io/${{ github.repository }}:${{ github.ref_name }}

      - name: Wait for health check
        run: |
          for i in $(seq 1 30); do
            if curl -sf http://localhost:31415/healthz; then
              echo ""
              echo "Container is healthy"
              exit 0
            fi
            echo "Waiting... ($i/30)"
            sleep 2
          done
          echo "Health check timed out — container logs:"
          docker logs calypso
          exit 1

      - name: Stop test container
        if: always()
        run: docker rm -f calypso || true

      - name: Push image
        run: |
          docker push ghcr.io/${{ github.repository }}:${{ github.ref_name }}
          docker push ghcr.io/${{ github.repository }}:latest
