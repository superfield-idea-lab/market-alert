#!/usr/bin/env bash
set -euo pipefail

LOG_PATH="${CLAUDE_E2E_LOG_PATH:-/tmp/claude-e2e.log}"

printf "ARGS: %s\n" "$*" >> "$LOG_PATH"
if [[ "${1:-}" == "-p" ]]; then
  printf "PROMPT: %s\n" "$2" >> "$LOG_PATH"
fi

echo "Mocked Claude response for studio e2e."
